#include <stdio.h>
#include <stdlib.h>

int main(){

    /*
        36) El INSPT requiere de un programa para procesar notas de alumnos de un solo curso. Para ello se pide un programa que:

        a) En main se pida ingresar el número de curso, cantidad de clases totales y los legajos de alumnos a procesar
        
        b) Una función que reciba como mínimo un legajo por parámetro y las clases totales. Luego debe pedir por teclado la cantidad 
        de clases asistidas y las 3 notas de sus parciales. La función devuelve el porcentaje de asistencia.

        c) Otra función que reciba las notas y devuelva el promedio.
        
        d) Al final del programa, se debe emitir: la cantidad total de alumnos, el legajo con el mejor promedio (mostrar legajo y 
        promedio) y el legajo con mejor asistencia (mostrar legajo y asistencia).
    */



    printf("\n");
    system("pause");
    
    return 0;
}