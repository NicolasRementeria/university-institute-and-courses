def find_sum(**kwargs):
    return sum(kwargs.values())
    
print(find_sum())

# Enter the correct parameters inside find_sum() so that the output of the code is 9

print(find_sum(a=1, b=1, c=1, d=1, e=1, f=1, g=1, h=1, j=1))
