#include <stdio.h>
#include <stdlib.h>

int main()
{

    /*
        Desarrollar un algoritmo que permita calcular el área (a) de un segmento de círculo. 
        Análisis: Para calcular el área de un segmento de círculo lo primero que hay que hacer es 
        leer el valor del radio y leer el valor de x que es la distancia del centro al segmento. 
        Una vez leído dichos valores se calcula aplicando la fórmula respectiva y por último se emite el valor del área
    */

   // PARA CONTINUAR EL EJERCICIO FALTA ACLARACION


    //float radio, x, area;
    //printf("Ingresar valor del radio del circulo: ");
    //scanf("%f", &radio);
    //printf("Ingresar valor de \"x\" que es la distancia del centro al segmento: ");
    //scanf("%f", &x);
    //printf("Circulo | Radio: %.2f, X: %.2f, Area Segmento Circular: %.2f", radio, x, area);
    //return 0;
}